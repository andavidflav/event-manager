<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <link rel="stylesheet" href="/public/css/style.css">
</head>
<body class="auth-page login-page">
  <header class="navbar">
    <div>
      <h1 style="margin: 0; font-size: 24px; font-weight: bold;"><%= siteName %></h1>
      <p style="margin: 0; font-size: 14px; color: #f0f0f0;"><%= siteDescription %></p>
    </div>
    <a href="/" class="button">Back to Home</a>
  </header>
  <main class="main-container">
    <div class="form-box">
      <h2>Login</h2>
      <p>Log in to access your account. Don't have an account yet? Register below!</p>
      <form action="/login" method="POST">
        <label for="username">Username:</label>
        <input type="text" id="username" name="username" required>

        <label for="password">Password:</label>
        <input type="password" id="password" name="password" required>

        <% if (error) { %>
          <p class="error-message"><%= error %></p>
        <% } %>

        <button type="submit" class="button">Login</button>
      </form>
      <p>New to <%= siteName %>?</p>
      <a href="/register" class="button secondary-button">Register</a>
    </div>
  </main>
</body>
</html>
