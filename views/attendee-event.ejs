<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Event Details</title>
  <link rel="stylesheet" href="/public/css/style.css">
</head>
<body>
  <header class="navbar">
    <h1>Event Details</h1>
    <a href="/attendee" class="button">Back to Attendee Home</a>
  </header>
  <main class="container">
    <h2><%= event.title %></h2>
    <p><%= event.description %></p>
    <p>Event Date: <%= new Date(event.created_at).toLocaleString() %></p>

    <form action="/attendee/book-event/<%= event.event_id %>" method="POST">
      <label for="user_name">Your Name:</label>
      <input type="text" id="user_name" name="user_name" required>
    
      <label for="full_price">Full-Price Tickets:</label>
      <input type="number" id="full_price" name="full_price" min="0" max="<%= event.full_price_tickets %>" required>
    
      <label for="concession">Concession Tickets:</label>
      <input type="number" id="concession" name="concession" min="0" max="<%= event.concession_tickets %>" required>
    
      <button type="submit" class="button">Book Tickets</button>
    </form>

  </main>
</body>
</html>
